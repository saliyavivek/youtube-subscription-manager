<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>YouTube Subscription Manager</title>
    <link rel="stylesheet" href="css/index.css" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
    />
  </head>
  <body>
    <div class="main">
      <% if (!authed) { %>
      <div class="main-not-authed">
        <div class="not-authed-title">
          <p>Streamline Your YouTube Subscriptions</p>
        </div>
        <div class="not-authed-desc">
          <p>
            Your ultimate tool for effortlessly organizing and managing your
            YouTube subscriptions. Whether you're subscribed to hundreds of
            channels or just a few favorites, our platform helps you keep track
            of all your subscriptions in one place.
          </p>
        </div>
        <div class="not-auth">
          <p><i>You must be signed in to continue</i></p>
          <a href="<%= url %>" class="login-with-google-btn">
            Sign in with Google
          </a>
        </div>
      </div>
      <% } else { %> <% if (userInfo) { %>
      <div class="main-authed">
        <div class="authed-title">
          <p>
            Welcome, <%= userInfo.name %> <% if(userInfo.verified_email ===
            true) { %> <b>verified</b> <% } else { %> <b>unverified</b> <% } %>
          </p>
          <p>You are logged in as <%= userInfo.email %></p>
          <p>
            <a href="/execute">my subscriptions</a>&nbsp;
            <a href="/logout">logout</a>
          </p>
        </div>
      </div>
      <% } %> <% } %>
    </div>
  </body>
</html>
