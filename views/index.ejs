<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>YouTube Subscription Manager</title>
    <link rel="stylesheet" href="css/index.css" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
    />
  </head>
  <body>
    <div class="main">
      <% if (!authed) { %>
      <div class="main-not-authed">
        <div class="not-authed-title">
          <p>Streamline Your YouTube Subscriptions</p>
        </div>
        <div class="not-authed-desc">
          <p>
            Welcome to our YouTube Subscription Manager, your ultimate tool for
            effortlessly organizing and managing your YouTube subscriptions.
            Whether you're subscribed to hundreds of channels or just a few
            favorites, our platform helps you keep track of all your
            subscriptions in one place.
          </p>
        </div>
        <div class="not-auth">
          <a href="<%= url %>" class="login-with-google-btn">
            Sign in with Google
          </a>
        </div>
      </div>
      <% } else { %> <% if (userInfo) { %>
      <div class="main-authed">
        <div class="card">
          <div class="card-body">
            <img
              class="card-img"
              src="<%= userInfo.picture %>"
              alt="User Picture"
            />
            <div class="card-right">
              <h5 class="card-title"><%= userInfo.name %></h5>
              <p class="card-email"><%= userInfo.email %></p>
            </div>
          </div>
          <a href="/execute" class="btn btn-primary">Get My Subscriptions</a>
        </div>
      </div>
      <% } %> <% } %>
    </div>
  </body>
</html>
